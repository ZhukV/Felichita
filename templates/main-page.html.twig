{% extends 'html-layout.html.twig' %}

{% block body %}
    <div class="container-fluid pt-5">
        <div class="row">
            <div class="col-2">
                <ul class="menu-categories">
                    {% for category in categories %}
                        <li>{{ category.title }}</li>
                    {% endfor %}
                </ul>
            </div>

            <div class="col-10">
                {% for group in grouped_products %}
                    <div class="pt-3 pb-3 border-bottom product-category">
                        <div class="row">
                            {% for product in group.products %}
                                <div class="col-4">
                                    <div class="row align-items-center">
                                        <div class="col-3 text-center">
                                            <img width="64" src="{{ product.webImagePath }}"/>
                                        </div>

                                        <div class="col-6">
                                            <h5>{{ product.title }}</h5>

                                            <div class="fw-bolder">{{ product.subTitle }}</div>

                                            <div class="text-muted small">{{ product.ingredients }}</div>
                                        </div>

                                        <div class="col-3 text-end">
                                            <div class="fw-bolder">{{ product.price }} UAH</div>
                                            <div class="text-muted">{{ product.weight }} g</div>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}